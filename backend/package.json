{
  "name": "backend",
  "version": "1.0.0",
  "description": "The backend package that SPA calls for authentication with JWT.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./yarn.lock,.npmrc,./database_scripts --copy-files --presets=es2015",
	 "start-prod": "npm run build && node dist/server.js",
	 "start": "npx nodemon --ext js --ignore ./dist --exec yarn start-prod"
  },
  "keywords": [
    "JWT",
    "SPA",
    "React",
    "backend",
    "MySQL"
  ],
  "bugs": {
    "url": "https://github.com/MehmetKaplan/react-spa-jwt-authentication-boilerplate/issues"
  },
  "author": "Mehmet Kaplan",
  "license": "MIT",
  "dependencies": {
    "@types/bcrypt": "^3.0.0",
    "bcrypt": "^3.0.3",
    "date-and-time": "^0.6.3",
    "express": "^4.16.4",
    "jsonwebtoken": "^8.4.0",
    "mysql": "^2.16.0",
    "nodemailer": "^5.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.3"
  }
}
